# ⚙️ [SYSTEM] THE ENGINE: 자율 에이전트 워크플로우 (v1.0)

이 문서는 모든 AI 에이전트가 준수해야 하는 **표준 협업 프로토콜**이다. 모든 작업은 개별 에이전트의 독단적 판단이 아닌, 아래의 단계별 검증 절차를 거쳐 진행된다.

---

## 🔄 PHASE 1: 설계 및 정합성 검토 (PLANNING)
**전담 에이전트:** @Lead Architect, @UI/UX Designer

1. **로직 분석:** Architect는 `PRD.md`의 수학적 공식($$T_{avail}$$, $$\Delta$$ 등)을 분석하여 데이터 스키마(`types.ts`)를 설계한다.
2. **UI 설계:** Designer는 설계된 데이터를 사용자가 가장 적은 마찰로 입력/확인할 수 있는 인터페이스(Tailwind 기반)를 제안한다.
3. **산출물:** `docs/design_v1.md` 생성.
4. **Gatekeeper:** 사용자의 **'승인(Approve)'**이 떨어지기 전까지 다음 단계로 넘어가지 않는다.

---

## 🛠️ PHASE 2: 구현 및 단위 테스트 (CODING)
**전담 에이전트:** @Full-stack Developer

1. **환경 구축:** 승인된 설계에 따라 React 컴포넌트와 Firebase 스키마를 생성한다.
2. **로직 격리:** 모든 계산 로직은 `src/utils/engine/` 폴더 내에 순수 함수로 작성하여 테스트가 용이하게 한다.
3. **보고:** 기능 구현이 완료되면 수정된 파일 목록과 함께 `Implementation Complete` 메시지를 출력한다.

---

## 🧪 PHASE 3: 무한 수정 루프 (TESTING & QA)
**전담 에이전트:** @QA & Performance Tester, @Full-stack Developer

1. **수식 검증:** QA는 구현된 수식이 기존 엑셀의 결과값과 일치하는지 가상 데이터를 넣어 검증한다.
2. **에지 케이스 공격:** - 가용 시간이 마이너스가 되는 경우
   - 루틴 비중 합계가 100%를 넘는 경우
   - 네트워크가 끊긴 상태에서의 기록 등
3. **자율 수정:** 버그 발견 시 QA는 Developer에게 수정을 명령하며, 이 과정은 **사용자 개입 없이 QA가 'PASS'를 선언할 때까지 반복**된다.

---

## 🏁 PHASE 4: 최종 보고 및 커밋 (REVIEW)
**전담 에이전트:** All Agents

1. **최종 보고:** 모든 테스트를 통과하면 `architect`가 전체 기능 요약 보고서를 제출한다.
2. **자동 기록:** 작업 완료 후 `auto_commit.bat`을 실행하여 현재까지의 변경사항을 Git에 기록한다.
3. **사용자 확인:** 사용자가 최종 결과물을 브라우저에서 확인하고 `Done`을 선언하면 해당 에피소드를 종료한다.

---

## ⚠️ 에이전트 행동 강령 (Rules of Engagement)
- **자율성:** 단계 내에서의 사소한 결정은 자율적으로 내리되, `PRD.md`의 핵심 로직을 변경할 때는 반드시 사용자에게 묻는다.
- **토큰 절약:** 불필요하게 파일 전체를 다시 쓰지 말고, 변경이 필요한 부분만 정확히 수정한다.
- **문서화:** 모든 중요한 기술적 결정은 `docs/` 폴더 내에 기록으로 남긴다.

### 📢 실시간 소통 및 의사결정 규칙 (Communication Protocol)

1. **로그 자동 생성:** 모든 에이전트는 복잡한 추론 과정이나 기술적 선택이 발생할 경우 `docs/DECISION_LOG.md`에 실시간으로 기록한다. (팀장님이 언제든 열어볼 수 있게 함)
2. **의사결정 요청:** 아래 상황에서는 작업을 일시 중단하고 사용자에게 질문한다.
   - PRD에 명시되지 않은 새로운 기능을 추가해야 할 때
   - 두 가지 이상의 구현 방식 중 장단점이 확실히 갈릴 때
   - 사용자 경험(UX)에 큰 영향을 주는 정책을 정할 때
3. **태그 사용:** 질문 시에는 반드시 `[DECISION REQUIRED]`라는 머리말을 사용하여 사용자의 시선을 끈다.
4. **중간 브리핑:** 긴 작업(코딩 등)의 경우, 15분마다 현재 진행 상황과 다음 단계를 터미널에 한 줄 요약 보고한다.